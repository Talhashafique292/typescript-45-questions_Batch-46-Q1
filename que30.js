//In this program I asked username using prompt and then print message to members special message to admin so use if else
var usernames = ["admin", "Ahmed", "zakir", "abbas", "Akram", "jameel"];
var user = usernames[1];
user = user.toLowerCase();
if (user === usernames[0]) {
    console.log("Hello " + user + ", would you like to check the status?");
}
else if (user > usernames[0]) {
    console.log("Hello " + user + " Good Morning!");
}
var user = usernames[2];
user = user.toLowerCase();
if (user === usernames[0]) {
    console.log("Hello " + user + ", would you like to check the status?");
}
else if (user > usernames[0]) {
    console.log("Hello " + user + " Good Morning!");
}
var user = usernames[3];
user = user.toLowerCase();
if (user === usernames[0]) {
    console.log("Hello " + user + ", would you like to check the status?");
}
else if (user > usernames[0]) {
    console.log("Hello " + user + " Good Morning!");
}
var user = usernames[4];
user = user.toLowerCase();
if (user === usernames[0]) {
    console.log("Hello " + user + ", would you like to check the status?");
}
else if (user > usernames[0]) {
    console.log("Hello " + user + " Good Morning!");
}
var user = usernames[5];
user = user.toLowerCase();
if (user === usernames[0]) {
    console.log("Hello " + user + ", would you like to check the status?");
}
else if (user > usernames[0]) {
    console.log("Hello " + user + " Good Morning!");
}
var user = usernames[0];
user = user.toLowerCase();
if (user === usernames[0]) {
    console.log("Hello " + user + ", would you like to check the status?");
}
else if (user > usernames[0]) {
    console.log("Hello " + user + " Good Morning!");
}
export {};
